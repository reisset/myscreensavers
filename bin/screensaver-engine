#!/home/nick/screensaver/venv/bin/python3
"""Terminal screensaver executable."""

import sys
from pathlib import Path

# Add parent directory to path so we can import screensaver package
sys.path.insert(0, str(Path(__file__).parent.parent))

from screensaver.engine import main

if __name__ == "__main__":
    main()
